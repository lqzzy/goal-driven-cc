<!-- Static discipline for each worker iteration (see the plugin template). -->

- Target the specific failing check in the Judge output above.
- Implement `is_prime(n)` in `src/is_prime.py` for real — do not hardcode the
  test values, do not edit the tests or CRITERIA.sh.
- Run `bash .goal-driven/CRITERIA.sh` to confirm progress, then append one line
  to `.goal-driven/PROGRESS.md`.
