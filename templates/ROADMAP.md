# ROADMAP

<!-- Coarse phase plan, written once by goal-planner (MODE A) after the criteria
     are sealed. A small ordered set of phases (2–6). Each phase is executed by:
     goal-planner (MODE B, fine plan) -> goal-worker loop -> advance.
     Prefer criteria-mapped exit conditions so "phase done" is objective. -->

## Phase 1 — <name>
- Objective: <one line>
- Targets: <criteria this phase turns green, e.g. C1, C2>
- Exit condition: <e.g. "C1, C2 pass"> 
- Approach: <how>
- Risks: <unknowns>

## Phase 2 — <name>
- Objective:
- Targets:
- Exit condition:
- Approach:
- Risks:

<!-- add phases as needed; keep it coarse -->
