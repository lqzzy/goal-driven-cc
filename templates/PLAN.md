# PLAN — current phase

<!-- Fine-grained plan for the CURRENT phase only, (re)written by goal-planner
     (MODE B) just before the phase runs. goal-worker executes these steps in
     order; each step should move a specific [FAIL] criterion toward [PASS]. -->

Phase: <name from ROADMAP>
Exit condition: <which criteria must pass to finish this phase>

## Steps
1. [ ] <small, verifiable step> — targets <criterion> — <command/file hints>
2. [ ] <...>
3. [ ] <...>

<!-- On a re-plan after a stall, first line below states WHY it stalled and what
     changes this time. -->
